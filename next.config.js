/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  // basePath: '/asty&andra',
  // assetPrefix: '/asty&andra/',
  images: {
    unoptimized: true,
  },
}

module.exports = nextConfig